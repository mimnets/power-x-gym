import React from 'react';
import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <div>
            <NavBar></NavBar>
            <h1>This is header</h1>
        </div>
    );
};

export default Header;